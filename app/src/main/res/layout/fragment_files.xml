<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg">

    <!-- M3 top app bar (64dp) -->
    <LinearLayout
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="@dimen/space_xs"
        android:paddingEnd="@dimen/space_s"
        android:background="@drawable/bg_topbar"
        android:elevation="3dp">

        <!-- back to parent (hidden at root) -->
        <TextView
            android:id="@+id/back_btn"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:gravity="center"
            android:text="←"
            android:textColor="@color/ink"
            android:textSize="22sp"
            android:background="@drawable/ripple_circle"
            android:visibility="gone" />

        <!-- exit selection mode -->
        <TextView
            android:id="@+id/exit_sel_btn"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:gravity="center"
            android:text="✕"
            android:textColor="@color/ink"
            android:textSize="18sp"
            android:background="@drawable/ripple_circle"
            android:visibility="gone" />

        <!-- breadcrumb path (clickable segments) -->
        <HorizontalScrollView
            android:id="@+id/crumb_scroll"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:layout_marginStart="2dp"
            android:scrollbars="none"
            android:overScrollMode="never"
            android:fillViewport="true">

            <LinearLayout
                android:id="@+id/crumb_row"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:orientation="horizontal"
                android:gravity="center_vertical" />
        </HorizontalScrollView>

        <!-- selection-mode count title -->
        <TextView
            android:id="@+id/sel_count_text"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="6dp"
            android:text="已选中 0 项"
            android:textColor="@color/ink"
            android:textSize="17sp"
            android:textStyle="bold"
            android:visibility="gone" />

        <!-- normal-mode actions -->
        <TextView
            android:id="@+id/sort_btn"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:layout_marginStart="@dimen/space_s"
            android:gravity="center"
            android:text="⇅"
            android:textColor="@color/ink"
            android:textSize="17sp"
            android:background="@drawable/ripple_circle"
            android:contentDescription="排序" />

        <TextView
            android:id="@+id/select_btn"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:gravity="center"
            android:text="☑"
            android:textColor="@color/ink"
            android:textSize="17sp"
            android:background="@drawable/ripple_circle"
            android:contentDescription="多选" />

        <TextView
            android:id="@+id/refresh_btn"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:gravity="center"
            android:text="⟳"
            android:textColor="@color/ink"
            android:textSize="18sp"
            android:background="@drawable/ripple_circle"
            android:contentDescription="刷新" />

        <!-- selection-mode actions (replace sort/select/refresh) -->
        <TextView
            android:id="@+id/sel_all_btn"
            style="@style/TopAction"
            android:text="全选"
            android:visibility="gone" />

        <TextView
            android:id="@+id/sel_invert_btn"
            style="@style/TopAction"
            android:text="反选"
            android:visibility="gone" />

        <TextView
            android:id="@+id/sel_move_btn"
            style="@style/TopAction"
            android:text="移动"
            android:visibility="gone" />

        <TextView
            android:id="@+id/sel_del_btn"
            style="@style/TopAction"
            android:text="删除"
            android:textColor="@color/danger"
            android:visibility="gone" />
    </LinearLayout>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/file_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:paddingHorizontal="@dimen/space_m"
            android:paddingTop="@dimen/space_s"
            android:paddingBottom="@dimen/space_l" />

        <com.google.android.material.progressindicator.CircularProgressIndicator
            android:id="@+id/loading"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:indeterminate="true" />

        <TextView
            android:id="@+id/empty_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="此目录为空 · 点击右上角 ⟳ 刷新"
            android:textColor="@color/muted"
            android:textSize="13sp"
            android:visibility="gone" />

        <TextView
            android:id="@+id/error_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:textColor="@color/danger"
            android:textSize="13sp"
            android:maxWidth="280dp"
            android:gravity="center"
            android:visibility="gone" />
    </FrameLayout>
</LinearLayout>